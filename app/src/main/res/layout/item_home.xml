<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

        <TextView
            android:id="@+id/des_text"
            android:layout_marginTop="13dp"
            android:textSize="20sp"
            app:layout_constraintTop_toBottomOf="@+id/news_txt"
            android:text="SpaceX раскрывает конкретные подробности о первом испытательном орбитальном полете Starship"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

       <ImageView
           android:id="@+id/new_image"
           app:layout_constraintLeft_toLeftOf="parent"
           android:layout_marginTop="5dp"
           app:layout_constraintTop_toBottomOf="@+id/des_text"
           android:src="@drawable/kosmos"
           android:layout_width="250dp"
           android:layout_height="150dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>